<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    android:label="@string/fragment_weight_tracker"
    app:startDestination="@id/weightTracker">

    <fragment
        android:id="@+id/weightTracker"
        android:name="com.example.android.weightlosstracker.ui.weigthtracker.WeightTrackerFragment"
        tools:layout="@layout/weight_tracker_fragment" >
        <action
            android:id="@+id/action_weightTracker_to_addWeightFragment"
            app:destination="@id/addWeightFragment" />
    </fragment>
    <fragment
        android:id="@+id/addWeightFragment"
        android:name="com.example.android.weightlosstracker.ui.addweight.AddWeightFragment"
        android:label="@string/fragment_add_weight"
        tools:layout="@layout/add_weight_fragment" />
</navigation>